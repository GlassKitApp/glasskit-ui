{
  "name": "@glasskit/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the GlassKit UI registry to AI agents (list + fetch additive-lens components).",
  "keywords": ["glasskit", "mcp", "model-context-protocol", "registry"],
  "homepage": "https://ui.glasskit.app",
  "bin": {
    "glasskit-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  }
}
