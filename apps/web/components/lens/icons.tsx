/**
 * Lens-preview glyphs — re-exported from Tabler Icons (a real icon library,
 * stroke-based so they inherit the `.gk-icon svg` stroke styling: currentColor,
 * round caps, our stroke-width). One mapping file, so swapping the icon set (or
 * an individual glyph) is a one-line change here.
 *
 * The registry components stay icon-agnostic — they take an `icon` ReactNode, so
 * consumers bring whatever icon library they like; this is only the docs demos.
 */
export {
  IconHeart as HeartGlyph,
  IconCheck as CheckGlyph,
  IconBattery as BatteryGlyph,
  IconChevronRight as ChevronGlyph,
  IconNavigation as NavGlyph,
  IconMessage as MessageGlyph,
  IconAlertTriangle as AlertGlyph,
  IconMusic as MusicGlyph,
  IconPhone as PhoneGlyph,
  IconPhoneOff as PhoneOffGlyph,
  IconCamera as CameraGlyph,
  IconMicrophone as MicGlyph,
  IconSun as SunGlyph,
  IconVolume as VolumeGlyph,
  IconKeyboard as KeyboardGlyph,
  IconPlayerPlayFilled as PlayGlyph,
  IconPlayerPauseFilled as PauseGlyph,
  IconPlayerTrackNextFilled as TrackNextGlyph,
  IconPlayerTrackPrevFilled as TrackPrevGlyph,
} from "@tabler/icons-react";
