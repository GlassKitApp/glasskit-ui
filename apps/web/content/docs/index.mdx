---
title: Getting started
description: GlassKit UI — the React component library for Meta Ray-Ban Display apps. Install the SDK, then vendor 44 premium HUD components with the CLI.
---

GlassKit UI is a React component library for **Meta Ray-Ban Display** apps. It
ships an SDK (the focus engine, the Neural Band hooks, `GlassViewport`, and the
on-lens stylesheet) plus **44 styled components** you vendor and own — built to
the premium-surfaces HUD language (calm dark base, popping gradient surfaces,
gradient icon plates, glanceable type).

## 1. Install the SDK

react and react-dom 19 are peer dependencies.

```package-install
@glasskit/glasses-ui
```

## 2. Import the styles

In your app's CSS entry, right after Tailwind:

```css
@import "tailwindcss";
@import "@glasskit/glasses-ui/styles.css";
```

## 3. Build

Wrap your app in `GlassViewport` and call `useDpad()` once near the root. Any
element with the `focusable` class joins D-pad navigation.

```tsx
import { GlassViewport, useDpad } from "@glasskit/glasses-ui";

export function App() {
  useDpad(); // arrow keys → spatial focus, Enter → click

  return (
    <GlassViewport>
      <div className="screen">
        <h1>Hello, glasses</h1>
        <button type="button" className="focusable">
          Get directions
        </button>
      </div>
    </GlassViewport>
  );
}
```

## 4. Add components

Vendor the source into your project with the CLI — it resolves dependencies and
writes the files to `components/glasskit/`, yours to edit.

```bash
npx glasskit add screen readout button
```

Browse all **44** in the [Components](/docs/components) section — each with a
live 600×600 preview, props, and usage. Click the **Meta logo** on any preview
to scan a QR and add it to your glasses.
