---
title: "ErrorState"
description: "A recoverable error screen: optional glyph, title, message, and a retry action. No red: the lens has one accent, so the failure reads from the words. Pairs with AsyncView's error slot."
---

<ComponentDoc slug="error-state" />
