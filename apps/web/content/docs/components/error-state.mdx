---
title: "ErrorState"
description: "A recoverable error screen: optional glyph + title + message + a retry action. No red \u2014 the lens has one accent, so the failure reads from the words. Pairs with AsyncView's error slot."
---

<ComponentDoc slug="error-state" />
