---
title: "Button"
description: "A D-pad-focusable action. Renders a real <button> with the focusable class, so useDpad walks it and activates it on Enter/Space. Edge + focus ring are emitted light, never fills."
---

<ComponentDoc slug="button" />
